# AGENT.md - 项目上下文与开发指南（交接文档）

## 项目概述

**项目名称**：自动测试
**项目路径**：F:\测试项目\自动测试
**项目类型**：工控类自动化测试软件（电子拼板 FCT 测试）
**技术栈**：C# .NET 8.0 Windows Forms（SDK风格项目）
**项目设置**：Nullable enable、ImplicitUsings enable、AllowUnsafeBlocks true
**NuGet包**：Microsoft.Data.Sqlite 8.0.0、System.IO.Ports 8.0.0
**相机SDK**：海康机器人 MvCameraControl.Net（Libs/MvCameraControl.Net.dll）
**显示器**：21.5寸 = 1920×1080，所有窗体等比例缩放适配
**IDE**：Visual Studio 2022（全程运行中）

## ⚠️ 关键工作规则（强制）

1. **VS 全程运行并会自动写回 Designer.cs** → 编辑任何文件前必须先用工具重新读取该文件；编辑完成后必须提醒用户在 VS 中"全部重新加载"
2. **不允许修改界面布局** — 布局由用户确认后固定，只改属性和逻辑（除非用户明确授权）
3. **按钮操作代码写入按钮事件中** — 逻辑集中在 Click 事件方法里
4. **中文命名** — 变量、方法、控件全部中文命名
5. **不加代码注释**（除非用户明确要求）
6. **代码简洁**，先实现功能再提供清单确认
7. **构建命令**：`dotnet build 自动测试.csproj`（在 F:\测试项目\自动测试 下执行）
8. 项目是 git 仓库（勿主动提交，除非用户要求）

## 目录结构（已按功能分组，勿打乱）

```
自动测试/
├── Program.cs                      // 入口，启动 Form1
├── 自动测试.csproj                 // SDK风格，自动包含所有子目录 .cs
├── AGENT.md / 开发规范.md
├── 主界面/
│   └── Form1.cs/.Designer.cs/.resx // 主窗体：相机初始化、菜单导航、加载配置、进入自动测试
├── 系统设置/
│   ├── 系统设置页面.cs             // 设置主页面，含保存/取消按钮
│   ├── 基础参数控件.cs             // 测试类型/串口设置/程控电源组/电源设置按钮
│   ├── 检测设置控件.cs / 运动控制控件.cs / 电压模块控件.cs
│   ├── MESS设置控件.cs / 其他设置控件.cs
├── 编辑配置/
│   └── 编辑配置窗体.cs             // 含嵌套类 配置项数据/检测项数据（测试流程数据模型）
├── 自动测试界面/
│   └── 自动测试界面.cs             // 测试执行入口：开始测试按钮已实现
├── 端口测试/ 端口测试页面.cs
├── 日志/ 日志页面.cs
├── 视觉调试/
│   ├── 视觉调试页面.cs / 相机属性.cs / 相机属性页面.cs
├── 电源控制/
│   ├── 一迈电源控制.cs             // Modbus RTU 通讯类（详见下文）
│   └── 一迈电源设置窗体.cs         // 调试用窗体（不保存参数）
├── 公共/
│   ├── 界面缩放器.cs               // 21.5寸等比例缩放
│   ├── 日志管理器.cs               // 日志记录（日志类别/权限等级）
│   ├── 系统配置数据.cs             // 系统配置.json 模型 + 获取可用地址列表()
│   ├── 配置数据.cs / 配置数据库.cs // SQLite（配置数据库.db：配置表/检测项表）
│   ├── 配置管理器.cs / 模块配置数据.cs / 模块寄存器配置.cs/.json
└── Libs/                           // 海康相机SDK
```

## 核心设计模式

- **单例模式**：配置管理器、配置数据库、系统配置管理、模块配置管理
- **配置驱动测试**：一个"配置"= 一套测试流程（配置项数据），内含若干"检测项"（检测项数据），按"排序"字段顺序执行，存于 SQLite 检测项表
- **PLC风格地址(IEC 61131-3)**：DO/VD/VA/CD/CA/PI/PO/PS 前缀+模块号.通道号，见 系统配置数据.cs 的 获取可用地址列表()

## 硬件架构（1+8+3）

- 1×串口通讯板（固定）— 与DUT串口通信
- 8×RS485功能板（可组合）— 电压模块页面配置类型
- 3×电源控制板（固定）— 上下电+采集电压/电流/功率
- 另有：一迈 YM600-Y60-L15 程控电源（Modbus RTU，站地址20，8N1，波特率默认9600）

## 检测项类型（12种，字符串定义于 编辑配置窗体.Designer.cs 类型列下拉框）

| 类型 | 地址前缀 | 设定值 | 最大/最小值 | 执行状态 |
|------|---------|--------|------------|---------|
| 继电器输出 | DO | 布尔 | 不需要 | ❌暂未接入硬件 |
| 直流电压 | VD | 数值 | 需要 | ❌暂未接入硬件 |
| 交流电压 | VA | 数值 | 需要 | ❌暂未接入硬件 |
| 直流电流 | CD | 数值 | 需要 | ❌暂未接入硬件 |
| 交流电流 | CA | 数值 | 需要 | ❌暂未接入硬件 |
| PWM检测 | PO | 数值 | 需要 | ❌暂未接入硬件 |
| 声音检测 | PI | 数值 | 需要 | ❌暂未接入硬件 |
| 电源输出 | PS | 布尔 | 需要 | ❌暂未接入硬件 |
| 输入功率 | PW | 数值 | 需要 | ❌暂未接入硬件 |
| 串口输出 | 无 | 文本 | 不需要 | ❌暂未接入硬件 |
| 相机检测 | 无 | 文本 | 不需要 | ❌暂未接入硬件 |
| 程控电源 | 无(Modbus) | 布尔(true开/false关) | 最大=电压(V)，最小=电流(A) | ✅已实现 |

类型行为分支在 编辑配置窗体.cs 的 更新行显示根据类型()：
- 需要数值范围 = 直流电压/交流电压/直流电流/交流电流/声音检测/PWM检测/输入功率
- 需要布尔值 = 继电器输出/电源输出/程控电源
- 保留最大最小 = 电源输出/输入功率/程控电源

## 一迈电源 Modbus RTU（一迈电源控制.cs，已完成）

- 协议：功能码 03(读)/06(写单)/10(写多)、CRC16(0xA001)、异常码识别
- 数据格式：float/UINT32 低字在前（CDAB）
- 控制字：读-改-写
- 站地址默认 20，ReadTimeout/WriteTimeout = 1000ms
- ⚠️ 踩坑记录：帧数组必须为 CRC 预留 2 字节（曾因 6 字节帧导致 CRC 覆盖数据字段→电源不回应→超时）
- 寄存器表（保持寄存器）：
  - 0x0000 状态字1、0x0001 状态字2、0x0002 内部温度
  - 0x0004 实际电压、0x0006 实际电流
  - 0x0009 控制字（bit0启动、bit1远程、bit6蜂鸣器、bit8-11 P1-P4）
  - 0x000A 设置电压、0x000C 设置电流、0x000E 限制功率（标配）/PWM周期（选配，双功能）
  - 0x0010/0x0012 PWM开通1/2
  - 0x0100 通信地址、0x0101 通信速率、0x0102 过压保护、0x0104 过流保护
  - 0x0108 默认限压、0x010A 默认限流、0x0112/0x0114 额定电压/电流
- 一迈电源设置窗体：仅调试用，**不保存参数**；连接时先读所有可写参数，失败自动断开；运行状态1秒定时刷新

## 自动测试执行逻辑（自动测试界面.cs，已实现）

- 加载配置(配置项数据)：填充检测项表格 + 同步板状态（拼板数，8列网格，PASS绿/FAIL红/默认灰）
- 开始测试按钮_Click：点击开始→后台 Task 按排序遍历启用的检测项→逐项执行+延时→全部完成后所有板 PASS；测试中再点=停止（CancellationTokenSource）；关闭界面自动取消+断开电源
- 执行程控电源(项)：首次执行用 系统配置管理.实例.基础参数.串口端口 + 程控波特率 连接（复用连接）；最大值→设置电压，最小值→设置电流（>0才设置）；设定值 true→启动电源/false→停止电源；每步记录日志
- 其他类型：记录日志"暂未接入硬件，跳过"
- 手动测试按钮：未实现

## 界面缩放器（公共/界面缩放器.cs）

- 等比例适配屏幕(Form)：按窗口总大小(含标题栏边框)与 Screen.PrimaryScreen.WorkingArea 计算比例
- 超出工作区则缩小适配（解决 1920×1080 Size 窗体被任务栏遮挡问题）
- 递归缩放控件 Location/Size/Font/表格列宽
- ⚠️ Anchor 含 Bottom/Right 的控件只缩放 Size 不缩放 Location（否则 Anchor 重算导致按钮位置错误/消失）
- 各窗体构造函数末尾调用 界面缩放器.等比例适配屏幕(this)

## 配置持久化

- 系统配置.json（公共/系统配置数据.cs）：基础参数类（测试类型/串口端口string/串口波特率/程控电源类型/程控波特率/各种开关）、运动控制类、电压模块类等
- 基础参数控件的端口框为 COM 下拉框（SerialPort.GetPortNames()），保存为 string
- 配置数据库.db（SQLite）：配置表 + 检测项表（含拼版1~32地址字段）
- 系统设置页面保存按钮 → 各控件.保存配置() → 系统配置管理.保存()

## 电源设置按钮（基础参数控件.cs）

```csharp
if (无程控框.Text == "一迈YM600-Y60-L15") { new 一迈电源设置窗体().Show(this); return; }
MessageBox.Show("未选择对应的程控电源");
```
后续添加其他程控电源时在此方法追加分支。

## 命名规范（保留有效）

| 控件类型 | 命名规则 | 示例 |
|---------|---------|------|
| Label | 功能+标签 | 主运一圈距离标签 |
| 输入框 | 功能+框 | 主运一圈距离框 |
| GroupBox | 功能组 | 主运组 |
| Button | 功能按钮 | 保存按钮 |

## 配色方案

- 深蓝 #2B579A（主色）、浅蓝 #5B9BD5（交互）
- 绿 #70AD47 / #00B050（正常）、红 #C00000 / #FF0000（异常）、橙 #ED7D31（提示）

## 当前开发状态

✅ 已完成：
- 主窗体、配置系统（JSON+SQLite）、各设置控件、编辑配置窗体
- VS设计器悬空字段修复、Visible=false修复、尺寸修复
- 界面等比例缩放（含Anchor修复、超屏缩小）
- 一迈电源 Modbus 通讯类 + 调试窗体
- COM口下拉框（基础参数控件 + 电源设置窗体）
- 测试类型"程控电源"（布尔设定值 + 电压/电流最大最小值）
- 开始测试按钮执行逻辑（程控电源真实执行，其他类型日志跳过）
- 文件按功能分目录重组
- 删除未使用的：其它模块控件、旧系统设置窗体

⏳ 待办/未实现：
1. 其余 11 种检测类型的硬件执行逻辑（继电器输出、直流电压等——需对接 RS485 功能板/电源控制板）
2. 手动测试按钮
3. 板级循环（每块拼板按各自拼版N地址执行）未细化
4. 电源控制是否接入自动测试流程（用户暂定先用调试窗体验证通讯）

## 注意事项

- 相机SDK依赖 Libs/MvCameraControl.Net.dll，csproj 已配置复制到输出目录
- 运行时生成：配置数据库.db、系统配置.json、模块配置.json（输出目录）
- .gitignore 已配置
- 端口测试页面.cs 中存在 nullable 警告（CS8622 等，48个警告属正常，0错误即可）
