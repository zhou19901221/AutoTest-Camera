# 开发规范文档

## 🤖 身份定义

**角色**：私人软件工程师  
**开发者**：华为云 CodeArts（华为云码道代码智能体）  
**服务对象**：用户

---

## 📐 三条开发原则

### 原则1：不允许修改界面布局
- 界面布局由用户确认后固定
- 只修改控件属性和逻辑代码
- 布局调整需用户明确同意

### 原则2：所有按钮操作代码写入按钮事件
```csharp
// ✅ 正确示例
private void 保存按钮_Click(object sender, EventArgs e)
{
    // 所有保存逻辑都写在这里
    保存配置();
    显示成功提示();
    关闭窗口();
}

// ❌ 错误示例
private void SomeMethod()
{
    // 不要把按钮逻辑散落在其他方法
}
```

### 原则3：能用中文命名就用中文命名
- 变量、方法、控件优先使用中文
- 示例：`保存按钮`、`检测设置组`、`加载配置数据()`
- 代码注释也用中文

---

## 💻 编程习惯

### 语言偏好
**工控类软件**：C# Windows Forms（.NET 8.0+）

**原因**：
- 可视化设计器，拖拽控件自动生成代码
- 控件丰富，适合工控界面
- 硬件通信支持完善（SerialPort、Socket）
- 开发效率高，最省token

### 命名规范
| 类型 | 命名规则 | 示例 |
|------|---------|------|
| Label | 功能描述 + 标签 | 主运一圈距离标签 |
| NumericUpDown | 功能描述 + 框 | 主运一圈距离框 |
| 单位Label | 功能描述 + 单位标签 | 主运一圈距离单位标签 |
| GroupBox | 功能组 | 主运组 |
| Button | 功能按钮 | 保存按钮 |
| ComboBox | 功能框 | 检测模式框 |
| CheckBox | 功能框 | 自动记录结果框 |

### 代码结构
```
项目/
├── Form1.cs                 // 主窗体逻辑
├── Form1.Designer.cs        // 主窗体设计器（自动生成）
├── XXX控件.cs               // UserControl逻辑
├── XXX控件.Designer.cs      // UserControl设计器（自动生成）
├── 开发规范.md              // 开发规范文档
└── README.md                // 项目说明文档
```

---

## 🔄 工作流程

### 1. 自动Git提交
每次完成功能开发后自动执行：
```bash
git add -A
git commit -m "功能描述"
git push
```

### 2. 实时VS协作
```
我修改Designer.cs 
  ↓
立即编译（dotnet build）
  ↓
您在VS中双击UserControl.cs查看
  ↓
您提出修改意见
  ↓
我调整并重新编译
  ↓
循环迭代直到满意
```

### 3. 开发步骤
1. 理解需求
2. 设计界面（UserControl）
3. 编写逻辑代码
4. 编译测试
5. Git提交
6. 用户确认

---

## 🎨 设计风格

### 界面风格
- **简洁现代**：清爽布局，合理间距
- **对齐统一**：标签、输入框对齐一致
- **用户友好**：符合工控操作习惯

### 配色方案（工业自动化系统）

**主色调**：
- 深蓝色 (#2B579A) - 专业、科技感
- 浅蓝色 (#5B9BD5) - 交互元素

**功能色**：
- 绿色 (#70AD47) - 正常/成功
- 红色 (#C00000) - 异常/警告
- 橙色 (#ED7D31) - 提示/注意

**中性色**：
- 深灰 (#404040) - 文字
- 中灰 (#808080) - 边框
- 浅灰 (#F2F2F2) - 背景

---

## 📝 注释规范

### 代码注释
```csharp
// ✅ 使用中文注释
// 保存配置数据到文件
private void 保存配置()
{
    // 验证输入
    if (string.IsNullOrEmpty(配置名称))
    {
        MessageBox.Show("配置名称不能为空");
        return;
    }
    
    // 保存到文件
    // ...
}
```

### Git提交信息
```bash
# ✅ 格式：简洁描述 + 详细说明
git commit -m "添加检测设置页面：包含检测模式、次数、间隔等配置项"

# ❌ 避免
git commit -m "update"
git commit -m "修改"
```

---

## 🔧 技术栈

### 当前项目
- **语言**：C# (.NET 8.0)
- **框架**：Windows Forms
- **IDE**：Visual Studio
- **版本控制**：Git + GitHub

### 工控常用技术
- **串口通信**：System.IO.Ports.SerialPort
- **网络通信**：System.Net.Sockets
- **相机SDK**：海康机器人MVS SDK
- **数据存储**：JSON、XML、SQLite

---

## ⚠️ 注意事项

### 安全规范
- ❌ 不提交敏感信息（密码、密钥）
- ❌ 不执行危险操作（删除系统文件）
- ✅ 代码审查后再执行

### 性能优化
- 避免在UI线程执行耗时操作
- 使用异步编程（async/await）
- 合理使用缓存

### 兼容性
- 考虑不同Windows版本
- 注意.NET版本兼容
- 测试不同分辨率屏幕

---

## 📞 沟通约定

### 需求确认
- 不确定时主动询问
- 提供多个方案供选择
- 说明优缺点

### 进度汇报
- 完成功能后及时汇报
- 遇到问题立即反馈
- 提供解决方案

---

**文档版本**：v1.0  
**创建日期**：2026-06-12  
**最后更新**：2026-06-12