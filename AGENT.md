# AGENT.md - 项目上下文与开发指南

## 项目概述

**项目名称**：自动测试  
**项目类型**：工控类自动化测试软件  
**技术栈**：C# .NET 8.0 Windows Forms  
**数据库**：SQLite (Microsoft.Data.Sqlite 8.0.0)  
**相机SDK**：海康机器人 MvCameraControl.Net  
**IDE**：Visual Studio 2022  

## 架构

```
自动测试/
├── Program.cs                    // 入口，启动 Form1
├── Form1.cs                      // 主窗体：相机初始化、菜单导航、操作日志
├── 配置管理器.cs                  // 单例，JSON配置加载/保存/备份
├── 配置数据.cs                    // 配置数据模型（基础参数/检测/运动/电压/MESS/其他）
├── 配置数据库.cs                  // SQLite数据库操作（配置表/检测项表，支持拼版1-32地址）
├── 模块配置数据.cs                // 模块配置管理（输入/继电器模块，JSON持久化）
├── 系统配置数据.cs                // 系统配置全量数据模型 + 地址生成（I/Q/R/AI/S前缀）
├── 编辑配置窗体.cs                // 配置编辑对话框
├── 系统设置.cs / 系统设置页面.cs  // 系统设置界面
├── 视觉调试页面.cs                // 视觉调试界面
├── 相机属性.cs / 相机属性页面.cs  // 相机属性设置
├── 基础参数控件.cs                // UserControl：基础参数配置
├── 检测设置控件.cs                // UserControl：检测设置配置
├── 运动控制控件.cs                // UserControl：运动控制配置
├── 电压模块控件.cs                // UserControl：电压模块配置
├── MESS设置控件.cs               // UserControl：MESS设置配置
├── 其他设置控件.cs               // UserControl：其他设置配置
├── 其它模块控件.cs               // UserControl：其它模块配置
├── Libs/                         // 海康相机SDK DLL
└── 开发规范.md                    // 开发规范文档
```

## 核心设计模式

- **单例模式**：配置管理器、配置数据库、模块配置管理、系统配置管理均使用单例
- **UserControl拆分**：各配置模块独立为UserControl，主窗体/设置页面组合使用
- **PLC风格地址(IEC 61131-3)**：DO(继电器输出) VD(直流电压) VA(交流电压) CD(直流电流) CA(交流电流) PI(脉冲输入/声音) PO(脉冲输出/PWM) PS(电源控制)，格式：前缀+模块号.通道号

## 开发原则（强制）

1. **不允许修改界面布局** — 布局由用户确认后固定，只改属性和逻辑
2. **按钮操作代码写入按钮事件** — 逻辑不散落，集中在Click事件中
3. **中文命名优先** — 变量、方法、控件用中文（如 保存按钮、加载配置数据()）
4. **匈牙利命名法** — Label用lbl前缀，控件按类型加前缀
5. **PLC命名约定(IEC 61131-3)** — DO(继电器输出) VD(直流电压) VA(交流电压) CD(直流电流) CA(交流电流) PI(脉冲输入) PO(脉冲输出) PS(电源控制)

## 命名规范

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| Label | 功能描述 + 标签 | 主运一圈距离标签 |
| NumericUpDown | 功能描述 + 框 | 主运一圈距离框 |
| 单位Label | 功能描述 + 单位标签 | 主运一圈距离单位标签 |
| GroupBox | 功能组 | 主运组 |
| Button | 功能按钮 | 保存按钮 |
| ComboBox | 功能框 | 检测模式框 |
| CheckBox | 功能框 | 自动记录结果框 |

## 配色方案

- 深蓝 #2B579A（主色）、浅蓝 #5B9BD5（交互）
- 绿 #70AD47（正常）、红 #C00000（异常）、橙 #ED7D31（提示）
- 深灰 #404040（文字）、中灰 #808080（边框）、浅灰 #F2F2F2（背景）

## 构建与运行

```bash
dotnet build
dotnet run
```

## Git工作流

- 每次完成功能后自动提交：`git add -A && git commit -m "描述" && git push`
- 提交信息格式：简洁中文描述 + 详细说明
- 换电脑工作时：先 `git pull` 同步最新代码

## 硬件架构(1+8+3)

- **1×串口通讯板**（固定）— 与DUT串口通信
- **8×RS485功能板**（可任意组合）— 类型在电压模块页面下拉选择
- **3×电源控制板**（固定）— 控制上下电+采集电压/电流/功率

### 功能板可选类型
- 无、脉冲声音模块、输出模块、直流电压模块、交直流电流模块（8）、交直流电流模块（16）、继电器模块

### 检测类型与地址前缀映射
| 前缀 | 含义 | 对应模块/检测类型 |
|------|------|-----------------|
| DO | 数字输出（继电器） | 输出模块 → 继电器输出 |
| VD | 直流电压 | 直流电压模块 → 直流电压 |
| VA | 交流电压 | 直流电压模块 → 交流电压 |
| CD | 直流电流 | 交直流电流模块 → 直流电流 |
| CA | 交流电流 | 交直流电流模块 → 交流电流 |
| PI | 脉冲输入 | 脉冲声音模块 → 声音检测 |
| PO | 脉冲输出 | 脉冲声音模块 → PWM检测 |
| PS | 电源控制 | 电源控制板 → 电源输出 |

### 检测类型完整表（10种）
1. 继电器输出(DO) 2. 直流电压(VD) 3. 交流电压(VA) 4. 直流电流(CD) 5. 交流电流(CA) 6. PWM检测(PO) 7. 声音检测(PI) 8. 电源输出(PS) 9. 串口输出 10. 相机检测

## 当前开发状态

- 主窗体框架已完成（菜单、相机初始化、日志）
- 配置系统已完成（JSON配置管理、SQLite数据库、模块配置）
- 各设置UserControl已创建（基础参数/检测/运动/电压/MESS/其他/其它模块）
- 编辑配置窗体已完成
- 视觉调试页面、相机属性页面已创建
- 电压模块控件.cs已重写完成（移除通道数概念，添加电源类型框列表，8功能板+3电源板模式）
- 电压模块控件所有控件已中文重命名（类型框/单位框/量程框/备注框/模块数标签/电源类型框）
- 编译通过（0错误）

## 注意事项

- 相机SDK依赖 Libs/MvCameraControl.Net.dll，需随项目复制到输出目录
- SQLite数据库文件：配置数据库.db（运行时生成在输出目录）
- 系统配置JSON：系统配置.json、模块配置.json（运行时生成）
- .gitignore 已配置，bin/obj/.vs/ 已忽略